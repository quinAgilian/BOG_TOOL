OTA说明
1. OTA函数, 使用一个输入参数，用于接受目标固件地址
2. OTA函数输出必要信息，与当前保持一致
3. OTA函数本身不做GUI，debug模式下，下拉框选择好之后，直接将固件地址传输给OTA函数，OTA直接执行。
4. 产测OTA步骤，直接调用产测rule中设定的目标固件地址。如果在check fw过程中判断不需要OTA。 那就置一个标记位。该标记位用来在OTA步骤中决定是否跳过OTA。
5. OTA输出的状态信息，到底使用DBG标签打印在日志区，还是FQC，取决于谁调用，换句话说，谁负责接收OTA函数输出，谁负责日志相关的业务。